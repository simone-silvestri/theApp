<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_better"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_better"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- The actual activity of the app -->
        <activity android:name=".Calendar" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".SignInActivity" />

        <activity
            android:name=".ImportWorkoutActivity"
            android:exported="true">
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.cfts" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.simone.cfts.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
        <activity
            android:name=".AddWorkoutActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity
        android:name=".ModifyWorkoutActivity"
        android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity android:name=".StringActivity" />
        <activity
            android:name=".ExerciseActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity android:name=".RepsInTimeActivity" />
        <activity android:name=".RepsActivity" />
        <activity android:name=".TimerActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".WorkoutActivity" />
        <activity 
            android:name=".MainActivity"
            android:exported="true"
            tools:node="merge">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>